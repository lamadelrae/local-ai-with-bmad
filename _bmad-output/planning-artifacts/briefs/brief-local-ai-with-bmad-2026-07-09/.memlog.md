---
updated: 2026-07-09T21:14
---

- (event) Update requested: add PDF/JPEG/PNG upload to chat, voice-to-text input mode, and local SQLite database for chat history/view. Conflicts with prior brief's explicit exclusions: 'no database anywhere in the stack' and 'persistence/history beyond in-memory session state' was Out of scope.
- (decision) Update in place (not forked). Driver: no single use case, wants general feature-parity with a more complete local chat app (uploads + voice + persistent history all equally core). Working mode: Coaching path.
- (decision) Vision: swap to a vision-capable local model (reverses brief's Gemma 4 E2B text-only premise; exec summary model identity will change). History: list + resume past conversations only (not search, not delete) via SQLite. Voice: input-only STT, no TTS.
