---
topic: local-gemma-chat PRD
updated: 2026-07-09T21:38
---

- (decision) Scope: single-page local chat app, Gemma 4 E2B via Dockerized llama.cpp OpenAI-compatible endpoint, Next.js + AI SDK + AI Elements + shadcn/ui
- (decision) Non-goal: no persistence/database — chat history lives only in browser/session memory (mirrors architecture AD-3)
- (decision) Non-goal: no auth, no multi-user, no cloud fallback, no Ollama in any form
- (decision) FR-1..FR-5 scoped to: inference server, chat API route, chat UI, server status affordance, local-only guarantee
- (event) PRD bootstrapped retroactively from final prd.md — no original discovery memlog existed for this run
- (decision) Scope pivot: add multimodal file attachment (JPEG/PNG/PDF), local voice-to-text, and persistent SQLite chat history — supersedes original brief non-goals (no persistence, no database) and architecture AD-3 (No persistence layer), per architecture's own 'revisit if a future need for history is explicitly scoped' escape hatch
- (decision) Voice-to-text must run on a local STT model (e.g. whisper.cpp-class), not browser Web Speech API, to preserve the existing local-only/zero-cloud-calls guarantee (FR-5)
- (decision) Image/PDF reading: native multimodal model input preferred over a separate OCR/extraction pipeline; exact mechanism (esp. for PDFs, which aren't natively vision-model input) deferred to architecture
- (decision) Chat history: full session management — multiple persisted conversations, list/switch/delete, not just a single continuous log
- (decision) Product Brief will be flagged for a follow-up update to its Non-Goals/Scope sections (via bmad-product-brief) so it stays consistent with this PRD; not edited by this run
- (change) Drafted FR-6 (multimodal file attachment), FR-7 (local voice-to-text), FR-8 (persistent SQLite storage), FR-9 (conversation history management); updated Goals/Non-Goals/NFRs/Success Metrics/Open Questions accordingly; frontmatter status reverted to draft pending finalize
- (assumption) Research (web search) confirms Gemma 4 E2B/E4B natively supports image and audio input via llama.cpp mtmd/mmproj through the same OpenAI-compatible endpoint — de-risks FR-6/FR-7, likely no separate vision or STT service needed; resolves prior architecture-blocking open question to 'probably fine, verify exact GGUF at build time'
- (event) Reviewer gate: rubric walker ran, verdict Adequate, no critical/high findings (review-rubric.md). Applied fixes: resolved FR-9 startup either/or (default to resume most recent), added FR-6..9 sequencing note (independent, any order), added Glossary section, clarified Success Metrics as manual eyeball checks, converted PDF-fallback assumption into a genuine Open Question
- (event) Polish pass applied (structure: relocated FR-6..9 sequencing note, condensed mmproj assumption; prose: fixed 'attachments' references' grammar). PRD finalized: status set to final
- (event) PRD finalized
